[settings]
task.run_auto_install = false

[tasks.check]
description = "Run dotfiles validation."
run = "bash scripts/test.sh"

[tasks.audit]
description = "Run the local dotfiles audit."
run = "bash scripts/audit-macos-dotfiles.sh"
