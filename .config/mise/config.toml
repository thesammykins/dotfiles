# Small global runtime baseline. Projects should declare heavier or tighter pins
# in their own mise.toml files.

[tools]
node = "24.14.0"
python = "3.13.12"
go = "1.26.1"
uv = "0.10.12"

[settings]
experimental = true
jobs = 4
