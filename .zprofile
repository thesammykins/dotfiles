#!/bin/zsh
# ~/.zprofile - Login shell configuration
# Runs once per login session; keep login-only environment here.

# Hermes Agent — ensure ~/.local/bin is on PATH
export PATH="$HOME/.local/bin:$PATH"
